import { DefaultTheme, css } from 'styled-components';

const colorTheme: DefaultTheme = {
  primary: '#E4D5C2',
  sub: '#5D8554',
};

const constants = {
  headerHeight: '66px',
};

export const theme = {
  constants,
  colorTheme,
};
